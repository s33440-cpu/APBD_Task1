APBD Task1 questions answers
1) f-f) When the branch being merged has all commits ahead of the current branch and the current branch has no new commits
 m-c) when both branches have new commits

2) Merge: Saves branch history and shows all commits
Rebase: Rewrites commits

3) I clicked the button "accept both changes" and it resolved, took me a few tries and aborts though

